# where to fetch the gems from
source 'https://rubygems.org'

# we're using github-pages, not native jekyll
gem 'github-pages', group: :jekyll_plugins

# our theme
gem "minima", "~> 2.0"

# our plugins
group :jekyll_plugins do
  gem "jekyll-feed", "~> 0.6"
end
